const Router = require('express')
const router = new Router()

router.post('/',) // Создание товара
router.get('/',) // Получение всех товаров  
router.get('/:id',) // Получение конкретного товара

module.exports = router