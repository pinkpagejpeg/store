const Router = require('express')
const router = new Router()

router.post('/registration',) // Регистрация
router.post('/login',) // Авторизация
router.get('/auth',) // Проверка на то, авторизован ли пользователь

module.exports = router